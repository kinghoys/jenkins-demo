pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                git url: 'https://github.com/kinghoys/jenkins-demo.git', branch: 'main'
            }
        }
        
        // Other stages...
    }
}
